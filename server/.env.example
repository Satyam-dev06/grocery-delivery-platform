# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/groceryhub

# JWT secret key (change this to a random string in production)
JWT_SECRET=groceryhub_secret_key

# Server port (default: 5000)
PORT=5000
